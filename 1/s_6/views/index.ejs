<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Form Submission</title>
    <link rel="stylesheet" href="stylesheets/style.css">
</head>
<body>
    <header>
        <h1>Submit Your Data</h1>
    </header>
    <main>
        <section>
            <form action="/submit" method="POST">
                <div>
                    <label for="title">Title:</label>
                    <input type="text" id="title" name="title" required placeholder="Enter title">
                </div>
                <div>
                    <label for="description">Description:</label>
                    <textarea id="description" name="description" required placeholder="Enter description"></textarea>
                </div>
                <div>
                    <button type="submit">Submit</button>
                </div>
            </form>
        </section>
        <section>
            <h2>Submitted Data</h2>
            <% if (file && file.length > 0) { %>
                <ul>
                    <% file.forEach(function(item) { %>
                        <li>
                            <h3><%= item %></h3>
                            <button><a href="/details/<%= item %>">View Details</a></button>
                            <button><a href="/edit/<%= item %>">Edit</a></button>
                        </li>
                    <% }); %>
                </ul>
            <% } else { %>
                <p>No data submitted yet.</p>
            <% } %>
        </section>
    </main>
    <footer>
        <p>&copy; <%= new Date().getFullYear() %> Your Company</p>
    </footer>
</body>
</html>